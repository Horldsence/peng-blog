# Database configuration
# PostgreSQL connection string format:
# postgresql://username:password@hostname:port/database_name
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/peng_blog

# Server configuration
# HOST: Server bind address (use 0.0.0.0 to listen on all interfaces)
# DO NOT use a domain name here - it will cause "Cannot assign requested address" error
# Valid values: 0.0.0.0 (all interfaces), 127.0.0.1 (localhost only), or specific IP
HOST=0.0.0.0
PORT=3000

# Base URL configuration
# BASE_URL: Public URL for OAuth callbacks and links (MUST use actual domain name in production)
# This is used by GitHub OAuth for redirect_uri and other public URLs
# Development: http://localhost:3000
# Production: https://yourdomain.com or https://your-subdomain.example.com
BASE_URL=http://localhost:3000

# Authentication configuration
JWT_SECRET=change-this-secret-in-production

# Storage configuration
UPLOAD_DIR=./uploads
CACHE_DIR=./cache

# GitHub OAuth configuration
# Step 1: Register your OAuth App at: https://github.com/settings/developers
# Step 2: Click "New OAuth App" and fill in:
#   - Application name: Your Blog Name
#   - Homepage URL: {BASE_URL} (e.g., https://yourdomain.com)
#   - Authorization callback URL: {BASE_URL}/api/comments/github/callback
#     Example: https://yourdomain.com/api/comments/github/callback
#   - Application description: (optional)
# Step 3: Copy the Client ID and Client Secret below
# IMPORTANT: The callback URL in GitHub settings MUST exactly match BASE_URL + "/api/comments/github/callback"
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Site configuration
ALLOW_REGISTRATION=true

# Log configuration
RUST_LOG=debug
RUST_LOG_SPAN_EVENTS=full
